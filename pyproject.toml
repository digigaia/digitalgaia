[project]
name = "DigitalGaia"
version = "0.1.0"
description = "DigitalGaia website"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sphinx",
    "myst-parser",
    "furo",
    "livereload",

    # unused now:
    # "sphinx-book-theme"
]
